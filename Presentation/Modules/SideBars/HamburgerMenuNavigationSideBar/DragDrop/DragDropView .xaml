<UserControl x:Class="Aksl.Modules.HamburgerMenuNavigationSideBar.Views.DragDropView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ei="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:views="clr-namespace:Aksl.Modules.HamburgerMenuNavigationSideBar.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>

        <Style x:Key="dragDropItemContainerStyle"
               TargetType="{x:Type ContentPresenter}">
            <Setter Property="Canvas.Left"
                    Value="{Binding Path=X}" />
            <Setter Property="Canvas.Top"
                    Value="{Binding Path=Y}" />
        </Style>

        <ItemsPanelTemplate x:Key="dragDropItemsPanelTemplate">
            <Canvas />
        </ItemsPanelTemplate>

        <DataTemplate x:Key="dragDropDataTemplate">
            <views:DragDropItemView />
        </DataTemplate>
    </UserControl.Resources>

    <ItemsControl x:Name="dragDropItemsControl"
                   AllowDrop="True"
                   Background="Transparent"
                   ItemsSource="{Binding DragDropItems}"
                   ItemsPanel="{StaticResource dragDropItemsPanelTemplate}"
                   ItemContainerStyle="{Binding Source={StaticResource dragDropItemContainerStyle}}"
                   ItemTemplate="{Binding Source={StaticResource dragDropDataTemplate}}">
        <ei:Interaction.Triggers>
            <ei:EventTrigger EventName="Drop"
                             SourceObject="{Binding ElementName=dragDropItemsControl}">
                <ei:CallMethodAction TargetObject="{Binding}"
                                     MethodName="ExecuteDrop" />
            </ei:EventTrigger>
            <!--<ei:EventTrigger EventName="PreviewMouseLeftButtonDown"
                             SourceObject="{Binding ElementName=dragDropItemsContro}">
                <ei:CallMethodAction TargetObject="{Binding}"
                                     MethodName="ExecutePreviewMouseLeftButtonDown" />
            </ei:EventTrigger>-->
            <ei:EventTrigger EventName="PreviewMouseMove"
                             SourceObject="{Binding ElementName=dragDropItemsControl}">
                <ei:CallMethodAction TargetObject="{Binding}"
                                     MethodName="ExecutePreviewMouseMove" />
            </ei:EventTrigger>
            <ei:EventTrigger EventName="PreviewMouseLeftButtonUp"
                             SourceObject="{Binding ElementName=dragDropItemsControl}">
                <ei:CallMethodAction TargetObject="{Binding}"
                                     MethodName="ExecutePreviewMouseLeftButtonUp" />
            </ei:EventTrigger>
        </ei:Interaction.Triggers>
    </ItemsControl>
</UserControl>
